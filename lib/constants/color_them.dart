import 'package:flutter/material.dart';

const backgroundColor = Colors.white10;
const appbarColor = Colors.white70;
const webBackgroundColor = Color.fromRGBO(18, 18, 18, 1);
const searchColor = Colors.white70;
const blueColor = Color.fromRGBO(36, 150, 227, 1);
const primaryColor = Color.fromRGBO(0, 0, 0, 1);
const secondaryColor = Color.fromARGB(255, 227, 222, 222);
